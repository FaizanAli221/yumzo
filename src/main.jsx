import React from "react";
import ReactDOM from "react-dom/client";
import YummzoApp from "../YummzoApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <YummzoApp />
  </React.StrictMode>
);
